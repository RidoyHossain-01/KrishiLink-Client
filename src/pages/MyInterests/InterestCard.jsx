import React from 'react';

const InterestCard = ({interest}) => {
     console.log(interest);
//      crop
// name,the owner of the crop, the quantity requested, the user’s message, and the
// status of the interest (e.g., pending, accepted, rejected).
     
     return (
          <div>
         
          </div>
     );
};

export default InterestCard;